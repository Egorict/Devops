---
- name: Full setup — Nginx + HTTPS + Docker proxy
  hosts: webservers
  become: yes
  vars_files:
    - vars/main.yml

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install Nginx, Certbot and required packages
      apt:
        name:
          - nginx
          - certbot
          - python3-certbot-nginx
          - curl
          - git
        state: present

    - name: Create web root directory
      file:
        path: /var/www/{{ domain_name }}
        state: directory
        owner: www-data
        group: www-data
        mode: '0755'

    # 4. СТАРТОВЫЙ КОНФИГ (без HTTPS)
    - name: Copy initial Nginx config (HTTP only)
      template:
        src: nginx-http.conf.j2
        dest: /etc/nginx/sites-available/{{ domain_name }}
      notify: restart nginx

    - name: Enable site
      file:
        src: /etc/nginx/sites-available/{{ domain_name }}
        dest: /etc/nginx/sites-enabled/{{ domain_name }}
        state: link
      notify: restart nginx

    - name: Remove default site
      file:
        path: /etc/nginx/sites-enabled/default
        state: absent
      notify: restart nginx

    # 7. ПОЛУЧЕНИЕ СЕРТИФИКАТА
    - name: Obtain SSL certificate from Let's Encrypt
      command: >
        certbot --nginx
        --domain {{ domain_name }}
        --non-interactive
        --agree-tos
        --email {{ letsencrypt_email }}
        --redirect
      args:
        creates: /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem
      notify: reload nginx

    # 8. ФИНАЛЬНЫЙ КОНФИГ (с HTTPS)
    - name: Copy final Nginx config with HTTPS
      template:
        src: nginx-https.conf.j2
        dest: /etc/nginx/sites-available/{{ domain_name }}
      notify: reload nginx

    - name: Set up automatic certificate renewal
      cron:
        name: "Renew Let's Encrypt certificates"
        job: "certbot renew --quiet && systemctl reload nginx"
        minute: "0"
        hour: "2"
        day: "*"
        month: "*"
        weekday: "0,4"

  handlers:
    - name: restart nginx
      service:
        name: nginx
        state: restarted

    - name: reload nginx
      service:
        name: nginx
        state: reloaded