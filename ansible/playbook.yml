---
- name: Test Ansible connection
  hosts: webservers
  become: yes
  tasks:
    - name: Ping test
      ping:

    - name: Install Nginx
      apt:
        name: nginx
        state: present
        update_cache: yes